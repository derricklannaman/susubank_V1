$('#calculations').append('<div class="box"><%= "Your susu, #{@susu_name}  \
                    will pay out $#{@total_hand} to each member, on a \
                    (#add payout frequency). All #{@members} members of the \
                    group will contribute $#{@member_contribution} \
                     (#add contribution frequency). The duration of the susu \
                     will be #{@duration} weeks." %></div>');

